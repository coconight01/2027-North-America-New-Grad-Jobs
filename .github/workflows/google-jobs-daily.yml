name: Google Jobs daily discovery

on:
  workflow_dispatch:
  schedule:
    - cron: "43 8 * * *"
  push:
    branches: [main]
    paths:
      - ".github/workflows/google-jobs-daily.yml"

permissions:
  contents: write

concurrency:
  group: update-jobs
  cancel-in-progress: true

jobs:
  google-jobs:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
    steps:
      - name: Google Jobs not configured
        if: env.SERPAPI_KEY == ''
        run: echo "SERPAPI_KEY is not configured; skipping optional Google Jobs discovery."

      - uses: actions/checkout@v4
        if: env.SERPAPI_KEY != ''
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        if: env.SERPAPI_KEY != ''
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        if: env.SERPAPI_KEY != ''
        run: python -m pip install -r requirements.txt

      - name: Validate collector syntax
        if: env.SERPAPI_KEY != ''
        run: python -m py_compile scripts/update_jobs.py scripts/extra_discovery.py

      - name: Discover Google Jobs
        if: env.SERPAPI_KEY != ''
        run: python scripts/extra_discovery.py --workers 5 --max-detail-pages 120 --skip-github

      - name: Commit changes
        if: env.SERPAPI_KEY != ''
        run: |
          if git diff --quiet; then
            echo "No Google Jobs changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md data/jobs.csv data/jobs.json
          git commit -m "chore: update jobs from Google Jobs"
          git pull --rebase origin main
          git push origin HEAD:main
